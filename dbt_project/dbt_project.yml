name: tarnijakataloogid_dbt
version: "1.0.0"
profile: tarnijakataloogid_dbt

seeds:
  tarnijakataloogid_dbt:
    +schema: marts

models:
  tarnijakataloogid_dbt:
    staging:
      +materialized: view
      +schema: staging
    intermediate:
      +materialized: view
      +schema: intermediate
    marts:
      +materialized: table
      +schema: marts
